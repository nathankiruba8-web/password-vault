{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm install --legacy-peer-deps && npx vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}